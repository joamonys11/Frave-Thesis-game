using UnityEngine;
using System.Collections;

public class freezedes : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Destroy (gameObject, 10f);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
