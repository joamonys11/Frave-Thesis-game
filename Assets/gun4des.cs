using UnityEngine;
using System.Collections;

public class gun4des : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Destroy (gameObject, 15f);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
