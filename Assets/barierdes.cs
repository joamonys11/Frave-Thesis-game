using UnityEngine;
using System.Collections;

public class barierdes : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
		Destroy (gameObject, 10f);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
